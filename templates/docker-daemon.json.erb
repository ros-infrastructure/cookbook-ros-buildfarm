{
  <%= if not @registry_mirrors.empty? then %("registry-mirrors": [ ) + @registry_mirrors.map{|m| %("#{m}")}.join(", ") + " ],\n" end -%>
  "seccomp-profile": "/etc/docker/seccomp/default-with-personality.json"
}
